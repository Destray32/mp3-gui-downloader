# mp3-gui-downloader

## Usage
  1. install dependencies and start with batch file if you want.
  2. Provide url into first box and press red button. After script executes you should have .mp3 file in 'pobrane-piosenki' directory.
